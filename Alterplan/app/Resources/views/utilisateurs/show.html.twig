<h1>Utilisateur</h1>

<table>
    <tbody>
        <tr>
            <th>Codeutilisateur</th>
            <td>{{ utilisateur.codeUtilisateur }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ utilisateur.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ utilisateur.prenom }}</td>
        </tr>
        <tr>
            <th>Isadministrateur</th>
            <td>{{ utilisateur.isAdministrateur }}</td>
        </tr>
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('utilisateurs_index') }}">Back to the list</a>
    </li>
    <li>
        <a href="{{ path('utilisateurs_edit', { 'codeUtilisateur': utilisateur.codeUtilisateur }) }}">Edit</a>
    </li>
    <li>
        {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </li>
</ul>
